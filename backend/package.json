{
  "name": "ai-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express backend proxying OpenRouter (chat completions) and Tavily (web search).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon --watch src --ext js,json src/server.js",
    "start": "node src/server.js",
    "lint": "eslint \"src/**/*.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "morgan": "^1.10.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
